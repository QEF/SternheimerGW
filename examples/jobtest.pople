#!/bin/tcsh
#$ -S /bin/tcsh
#$ -l qname=parallel.q
#$ -l h_rt=00:20:00
#$ -N jobtest
#$ -o jobtest.out 
#$ -e jobtest.err
#$ -cwd
#$ -pe impi2 8
#$ -j y
#$ -R y

set MPIRUN  = "/share/apps/intel/impi/3.2.0.011/bin64/mpirun -r ssh -n $NSLOTS -f $TMPDIR/machines"
set HOME    = /home/giustino
set GWHS    = $HOME/GWHS/trunk/bin/gwhs.x
set JDIR    = $HOME/GWHS/trunk/examples

$MPIRUN $GWHS -npool 8 > test.out

exit

