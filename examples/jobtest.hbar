#!/bin/tcsh
#PBS -l ncpus=8,nodes=2:dualcore:ppn=4
#PBS -k eo 
#PBS -V                 

/usr/local/bin/lamboot $PBS_NODEFILE

set NPROC = 8

set MPIRUN  = /usr/local/bin/mpirun
set HOME    = /home/giustino

set GWHS    = $HOME/GWHS/trunk/bin/gwhs.x
set JDIR    = $HOME/GWHS/trunk/examples

set TDIR    = /nfs/scratch/giustino/gwhs

cd $TDIR 
cp $JDIR/klist.dat ./
$MPIRUN -np $NPROC $GWHS -npool $NPROC > $JDIR/test.out

/usr/local/bin/lamhalt $PBS_NODEFILE

exit

